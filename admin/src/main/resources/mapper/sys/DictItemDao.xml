<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.supermap.modules.sys.dao.DictItemDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.supermap.modules.sys.entity.DictItemEntity" id="dictItemMap">
        <result property="dictItemId" column="dict_item_id"/>
        <result property="dictId" column="dict_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getByCodeAndDictName" resultMap="dictItemMap">
        select *
        from sys_dict_item
        where dict_id = (select dict_id from sys_dict where name = #{dictName})
        and code = #{code}
    </select>

    <select id="getByDictName" resultMap="dictItemMap">
        select *
        from sys_dict_item
        where dict_id = (select dict_id from sys_dict where name = #{dictName})
    </select>
</mapper>